import { helper } from "@ember/component/helper";

export function starRating(params /*, hash*/) {
  return params;
  {
    {
      star - rating;
      numStars = N;
      readOnly = false / true;
      baseColor = "#fff";
      fillColor = "#000";
      width = N;
      height = N;
    }
  }
}

export default helper(starRating);
